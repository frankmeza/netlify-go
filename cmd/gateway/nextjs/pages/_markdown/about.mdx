# some markdownx content

some markdownx text

### and another title too  
{props}

{props.tres}

```typescript
const cleanUp = (mess: Object) => {
    return Object.entries(mess)
        .map((key, value) => {
            console.log({ key, value })
        })
}
```
